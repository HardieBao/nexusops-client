# 内置 SkillOps 工具使用统计试用

本版 Windows x64 桌面端内置 Codex / Claude Code 的 Hook 采集，不需要单独安装 SkillOps 或 Node.js。

## 使用步骤

1. 把整个 ZIP 解压到固定目录，运行 `nexusops-client.exe`。本包为未签名试用程序。
2. 在 Team 页连接组织（或导入本人 Team Profile）。服务端必须先部署本次工具统计功能及迁移 `0264_employee_tool_usage.sql`；旧 UAT 不支持上报。
3. 在「工具使用统计 · SkillOps」点击「启用采集并上传」。这会备份并合并 Codex / Claude Code 的 Hook 配置，保留其他设置。
4. 重启 Codex / Claude Code，在 `/hooks` 中确认并信任新增 Hook。运行时版本或管理策略不支持 Hook 时，不会产生使用数据。
5. 执行一次真实交互，返回桌面端点击「立即上传」，或保持桌面端运行等待最多一分钟。
6. 管理员进入 NexusOps「用量与预算 → 员工工具使用」，按员工 ID、工具和时间筛选。

管理员可见工具名称、启动会话数、交互次数、完成轮次、工具调用次数和最后使用时间。不上传提示词、代码、对话、目录路径或原始 session ID。统计是客户端上报事件，不等于工作时长或效率。

## 停止、离线和配置变更

- 「关闭采集并清空待上传」会停用采集、清空队列，并移除本功能的 Hook。退出组织也会停用采集并清空队列。
- 断网时队列保留原始去重 ID；恢复后重试不会重复计数。最多保留 10,000 条待上传事件；满时停止新增并提示错误。超过 30 天的离线事件不再上传。
- 桌面关闭时 Hook 可以继续把元数据写入本机队列，重新打开桌面后上传。
- 切换 Claude 供应商可能替换设置；点击「修复 Hook（切换供应商后）」并重新确认。移动 EXE 或更换安装目录后也需修复 Hook。
- 连接其他员工身份时不会沿用此前的采集授权或待上传队列。
- 首版只支持 Codex 和 Claude Code 的工具使用统计，不包含完整 SkillOps 评测界面、具体 Skill 执行推断或 Cursor 采集。

## 本次验证

Team Rust 回归 55 项通过、4 项既有外部环境测试跳过；统计面板与 Team 页面 20 项测试通过。新增 HTTP 测试覆盖断网重试保留 ID、拒绝重定向、401 / 403、退出停止采集。已用生成的 release EXE 启动 13 次真实 Hook 进程，得到 8 条预期事件，确认隐私标记未写入 SQLite、错误输入正常退出、关闭后停止记录。

已验证 Hook 程序本身，但尚未替你在真实 Codex / Claude Code 账户中启用并确认 Hook。后台网页和接口改动在 NexusOps 的 `codex/team-ai-control-plane` 工作树，未自动合入主分支或部署 UAT。
