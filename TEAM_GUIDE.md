# NexusOps Team 使用指南

## 连接工作区

在 Gateway Console 的“我的 Key”中选择当前可恢复的成员 Key，并导出 Team Profile JSON。该文件包含凭据，应按密钥文件保管，不要提交到 Git、工单或聊天记录。

在 NexusOps Client 打开“团队”：

1. 选择 Team Profile JSON，或手工填写 Gateway 地址与完整成员 Key。
2. 核对地址后点击“验证并连接”。读取 JSON 本身不会自动联网。
3. 客户端用 Key 读取 profile、stable manifest 和授权下载；它不能执行上传、审批、发布或回滚。

首版同时只连接一个工作区。切换组织前先退出当前团队，再使用新组织的配置连接。

成员 Key 保存在操作系统凭据存储中；`team.db` 只保存工作区标识、安装基线、订阅状态和本地映射。Key 撤销、过期、成员停用或分组移除后，远端访问会停止并提示重新连接。

## 导入团队供应商

选择目标工具和 profile 中已授权的模型，然后先点“预览变更”。

- 首次导入只创建供应商，不会把它设为当前供应商。
- 重复导入复用同一个团队供应商，不增加重复项。
- Key 或模型变化需要再次预览并确认；更新已激活的团队供应商前，需要先切换到其他供应商。
- 预览、导入和激活前都会联网重新校验授权。预览后凭据、授权模型或本地供应商配置发生变化，旧确认会失效，需要重新预览；重启客户端或重新连接团队后也一样。
- 备注、排序和故障转移等个人字段会保留；团队供应商名称与连接字段由 Team 管理，个人供应商不会被团队导入覆盖。
- “设为当前供应商”是独立动作，会调用客户端原有切换流程并写入目标工具配置。

Gateway 地址可以带末尾 `/v1`；Claude 和 Gemini 的配置会使用保留反向代理前缀的根地址，由工具追加各自的协议路径。Antigravity 使用根地址下的 `/antigravity`，不会生成错误的 `/v1/antigravity`。已有团队配置可通过重新预览发现地址差异；更新已激活的供应商前，先切换到个人或官方配置，再确认团队更新。

供应商被目标工具使用时，其工具配置文件需要保存成员 Key。退出 Team 会清除操作系统凭据中的登录副本，但不会静默删除已经导入的供应商或工具文件。

## 同步团队资产

先选择目标工具，再点“预览同步”。列表会显示每项资产的安装方式和状态：未安装、无变化、远端更新、本地修改或双方修改。

- Skill 安装到目标工具的 Skill 目录，并登记到客户端原有 Skill 服务。
- Prompt 导入客户端原有 Prompt 列表，但保持未启用；启用仍需在 Prompt 页面明确操作。
- Rule、Workflow、Agent 下载到 Team 受管目录，不自动改写共享的 `CLAUDE.md`、`AGENTS.md` 等文件。
- 下载会校验 revision、正文哈希、Skill 归档哈希、文件清单、路径和大小限制。安装不执行包内脚本。
- 本地修改或双方修改默认跳过。只有逐项勾选“允许覆盖本地修改”后才会替换，并在替换前保存副本。
- 连续同步无变化时不重写文件；服务端 A → B 或回滚 B → A 都按新的 stable revision 同步。
- 单项失败不会把整批显示为成功。修复网络、文件占用或权限问题后重新预览并重试。

退出、离线或服务端撤权不会删除已下载文件。客户端把对应记录标记为不再同步；无法远程收回本地副本。

## 恢复与诊断

安装使用同一文件系统内的临时目录、journal 和上一版本副本。异常中断后，下次同步会先恢复未完成操作。若文件被占用或目标路径包含符号链接、junction、路径穿越或不安全归档项，安装会拒绝并保留上一有效版本。

调试日志只记录状态和非敏感标识，不记录 Authorization、完整 Key、Team Profile 或资产正文。排查问题时可提供错误码、资产 ID、revision 和脱敏截图。

## 当前身份和发布边界

当前版本使用成员 Key；OAuth device flow 尚未实现。Windows 已执行连接、OS 凭据存储、Provider、同步与回滚验证。macOS、Linux、平台签名、公证和自动更新的实际支持范围以对应发布证据为准。
