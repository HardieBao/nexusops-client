# 剩余验收与恢复执行条件

2026-09-12。结论：**部分完成，外部验收条件阻塞；不能宣布首版全部完成。** 原 T00—T23、M1—M5、S01—S30 范围不变。

## 当前版本和可复核证据

- 程序源码 / 包版本为 ce86a64e；后续提交为文档。包、哈希、1192 项前端测试、语言检查、安装 / 卸载与性能见 [最终试用包记录](evidence/package-and-native-ce86a64e.md)。
- Gateway / 管理端为 23a5657416db57938b78cf2c86a4533153a8220e，工作树干净。本轮再次请求 UAT health 返回该 revision、ok；未认证 projects 返回 401。
- 客户端 Rust、TeamAI 构建模块、固定第三方文件与锁文件自 4d33d55 后无 diff。实读已有 Rust 日志确认 2874 passed / 0 failed / 19 ignored；忽略项不能自动算通过。此为既有检查的源码适用性核对，不是本轮重跑 Rust。
- 实读 teamai-cross-go.log：Skill、Rule 两项 TestRustCandidatePublicationIntegration 均 PASS。源码确认使用真实 Rust 候选、生产授权 / 规范化 / 发布服务和 PostgreSQL，覆盖成员禁止上传、发布前不可见、Canary / 审批门槛、Stable 后成员下载与内容核对；blob 存储在内存中。它不是完整 UAT 链路。
- 原有 136 个组件文件、公共 hooks / API / query / config 未改；14 个具名原 App 操作回调均保留。见 [源码复核](evidence/legacy-preservation-20260912.md)。

## T03 收口

S01 的空环境引导、S02 的 15 类保存页面及回退、固定导航与快捷键已有原生和组件证据。App、ClientShell、navigation、ClientHomePage 自 2fa03d89 后未改，既有证据仍适用。

本轮补测真正的 Windows 标题栏：最大化后 is_maximized=true，还原后 false；拖动使位置从 (780,233) 到 (820,233)；最小化后 is_minimized=true，恢复后 false。第一次文本捕获缺少几何信息，最大化时又混入隐藏辅助窗口截图；选定实际主窗口截图后操作成功，未改产品代码。实际标题栏关闭与键盘操作另有前述记录。

在 GUI 中由 Claude 切换到 Codex，再进入使用记录、正常退出、启动新进程：app=codex、view=usage，代理 running=false，仍读到 4 条合成历史、660 Tokens、0.600000，固定导航保留。数据见 `evidence/window-navigation-ce86a64e.json`。据此 T03 验收完成；T10 的托盘门槛仍独立保留。

## 尚未完成的验收组

| 任务 | 当前证据与未完成条件 |
| --- | --- |
| T00—T03 | 已验收；不因此推进其他任务或整个 M2 |
| T04 | 共享上下文、取消和迟到响应已有测试；实际 UAT 身份切换联合验收待完成 |
| T05 | 个人配置保留与显式启用有测试；两种工具使用授权 UAT 配置的预览、取消、启用、检测待联调 |
| T06 | 个人 Prompt、候选导出、安装冲突和恢复已有局部证据；UAT 授权资产的完整版本 / 权限流待联调 |
| T07 | 采集与身份保护有测试；真实成员连接、启用 / 禁用 / 退出的最终联合验证待完成 |
| T08 | 新旧本机统计一致，偏好与四语言口径已验证；与真实成员 Hook 统计的联合验收仍需完成 |
| T09 | 空状态、观察状态和跳转有证据；UAT 断网、认证失效、资产冲突的最终联合状态待核对 |
| T10 | 旧入口、源码保留、Prompt、窗口、快捷键、文件选择与单实例有证据；托盘打开 / 退出仍待人工结果 |
| T11 | 固定模块与包内依赖已验证；新 Windows 用户 / 干净 VM 条件未验证 |
| T12 | 协议、只读权限、取消 / 超时 / 回收有测试；仍受 T11 整体环境门槛及联合验收约束 |
| T13 | 本地真实候选到生产发布服务 / PostgreSQL 已通过；UAT 管理端批准与成员获取链路待完成 |
| T14 | HTTP / 权限 / 持久化与兼容测试通过；最终实际 UAT 客户端请求仍需认证联调 |
| T15 | 安装、哈希、冲突、恢复、失败 ACK 与补确认有测试；两工具消费实际批准的 UAT revision 待完成 |
| T16 | 实际 CLI 各 3 事件通过，Hook 幂等和隔离有测试；最终包在 UAT 身份下的工具确认、上报待完成 |
| T17 | 本机历史独立于队列、ACK 后保留、身份隔离有测试；同一批 20 唯一事件 / 5 重传的两端对账待联合完成 |
| T18 | 管理权限 / 兼容已有测试；最终 UAT 管理员页面对员工统计及账单不变的核对待完成 |
| T19 | 本地故障注入已覆盖相关模块；最终真实身份、文件、队列与服务端状态联合演练仍需完成 |
| T20 | 语言、布局、快捷键与最终性能有证据；依赖场景未全部通过，不宣布整个 M4 完成 |
| T21 | 最终包已实际安装 / 启动 / 卸载；干净用户环境与已认证连接 / Hook 全链路未完成 |
| T22 | UAT 部署、备份、迁移和应用回退有证据；登录后的授权下载 / 管理统计仍待验证 |
| T23 | 试用说明、包和证据已交付；真实包 → CLI → UAT → 对应员工管理统计的最终验收未完成 |

## 必需外部输入

1. **已授权 UAT 测试管理员与成员身份**：提供可用测试会话，或本机凭据文件路径，并标明管理员 / 成员角色。当前受保护 API 实测 401，不能凭 SSH 管理权限伪造登录、重置密码或冒充成员。
2. **托盘人工结果**：关闭主窗口后通过托盘“打开主界面”，再用托盘“退出”。当前接口可显示菜单，但菜单输入没有可靠投递；Orca provider 明确 surfaces.menus=false。已请求结果，未收到回答。
3. **干净 Windows 验收环境**：已有环境只证明现有用户下的隔离应用数据和无工具 PATH 启动，不能将其改写为新 OS 用户 / VM 验收。需可用测试机器 / VM 或人工执行结果。

这些条件在多轮执行中持续缺失。可独立进行的实现修复、构建、源码核对和相关本机验证已完成；没有正在运行的构建或测试可等待。目标应保持“未完成并被外部条件阻塞”，不重复通过的检查来代替这些输入。

恢复后按 [试用说明](trial-guide.md) 顺序执行：连接与配置 → 候选审批 / Stable → 两工具安装与真实事件 → 20+5 对账 → 断网 / 冲突 / 禁用 / 身份切换 → 管理权限与回退，最后逐项复核 O1—O6、所有任务和硬性场景。任何当前“待验收”项都不会因阻塞而被自动视为通过。
