# 提示词原生交互与托盘观察

2026-09-12；T06 / T10 的局部验收。使用客户端 2fa03d89 release EXE（SHA256 fa0c48bb4d2ac8c006d38036cdd1dd9d35a000661e8cf6d93ef7dab79d1f0e38），隔离主目录、Claude / Codex 配置和 WebView；未连接组织或真实上游。没有修改产品源码。

## Claude 个人提示词完整流程

从固定资产库入口打开原有提示词页，使用实际 UI、Tauri IPC 与磁盘文件执行：

1. 首次列表为 0，Claude 生效文件不存在。
2. 新建并输入草稿时，四个主导航、两个次级入口及页头返回均禁用；明确使用编辑面板的返回后，列表仍为 0。截图 `native-prompt-edit-blocked.png` 为合成草稿。
3. 新建 Native QA prompt 并保存 V1，列表为 1、未启用，Claude 生效文件仍不存在。
4. 显式开启开关后，列表显示启用，实际 CLAUDE.md 内容等于 V1；SHA256 fcf2ab587eedfd8d27d2885cc83f587af56b9157bc1690bdfff0c89293f9bac3。
5. 编辑内容再返回，已启用文件哈希保持不变；重新打开编辑器仍为已保存 V1。
6. 最初使用自动化 fill 操作 CodeMirror 时得到 V2+V1 拼接，不能视作正确替换。改为聚焦编辑器、Ctrl+A、Backspace、type，在保存前读取编辑器确认只有 V2，再保存。实际文件精确等于 21 字节 `NEXUSOPS_QA_PROMPT_V2`，SHA256 3af02e51987bb149431ed85d5fe70540a2d265337f396123d661e5a00ab7615b。
7. 正常退出并启动新进程后，真实 get_prompts 读到 1 项，名称、V2 内容和 enabled=true 均保留。
8. 关闭开关后显示未启用，生效文件存在且长度为 0，符合当前禁用行为。
9. 删除对话框取消后记录仍为 1；再次删除并明确确认后，列表为空。正常退出后以只读 SQLite 检查隔离数据库，prompts 总数为 0，生效文件仍为空。

使用浏览器错误检查命令没有捕获到页面异常；这不抹去启动时更新端点的已有网络错误。未验证提示词经真实模型执行的效果，也不将一个编辑器的流程扩写为全部旧功能通过。

## 托盘观察与覆盖缺口

通过 Explorer 的任务栏窗口打开“显示隐藏的图标”，实际找到 NexusOps Client 图标。右键打开菜单，显示“打开主界面”、仓库入口、工具子菜单、轻量模式与“退出”；截图 `native-tray-menu-2fa03d89.png` 仅包含本产品菜单。

窗口关闭 API 已令主窗口隐藏。对菜单项的坐标点击未触发源码中的“处理托盘菜单事件”日志，主窗口也未恢复；重新观察和使用另一接口仍未形成可靠输入证据。本轮 Orca provider 1.0.0 明确返回 surfaces.menus=false、windows.focus=false，Sky 没有列出该弹出菜单为可操作窗口。因此只能确认菜单可显示，不能判定菜单功能已通过，也不能据此判为客户端缺陷。

为继续独立验收，通过已验证的单实例路径恢复主窗口，再完成上述提示词流程。已向用户请求实际验证托盘“打开主界面 / 退出”；未收到结果前继续保留 T10 托盘操作未验收。测试客户端最后正常退出，没有借托盘退出未验证路径宣称退出成功。

结构化结果见 `native-prompt-and-tray-2fa03d89.json`。剩余包含托盘操作、其他未覆盖的旧功能交互，以及已授权 UAT 身份下的发布、安装、工具上报和管理员统计对账。
