# T17 使用记录页面检查点

日期 2026-09-11；未提交工作树，T17 继续实施。

- ClientUsagePage 增加本人本机工具事件面板，调用历史读取/清理 IPC；原模型 UsageDashboard 保留，口径在页面明确区分。
- 按工具筛选，展示 UTC 日期、事件、计数、最近观察时间；空记录与失败分别显示，不展示成功率或模拟数值。
- 清理需点击二次确认，使用共享操作忙碌状态。连接 ID / Key ID 改变重建面板，旧异步结果不写入新身份视图；卸载后读取结果失效，没有新增计时轮询。
- 简中、繁中、英文、日文均新增对应翻译。

本轮验证：

1. 新增 3 项组件测试覆盖确认清理、迟到身份响应、错误/空记录和工具筛选。
2. `pnpm typecheck`、`pnpm test:unit`、`pnpm build:renderer` 最终通过；完整前端 147 文件、1178 项测试通过。首次类型检查因 replaceAll 不适用于既有目标失败，改用兼容正则替换后重跑通过。
3. 浏览器会话 history-ui 使用实际 App 和独立 IPC 夹具：展示合成 Codex 20 次、Claude Code 3 次；筛选正常；确认前清理调用数 0，确认后 1，返回空记录；页面 errors 为空，无横向溢出。截图 `tool-history-ui.png` 中数字均为夹具，不是员工统计，更不是 UAT 证据。
4. GitNexus 修改前 ClientUsagePage 为 LOW；累积 detect_changes 为 36 文件/180 符号/36 流程，CRITICAL。新文件及动态 IPC 的影响结合源码、类型、组件测试核对。diff 检查通过。

剩余：工作台接入摘要、实际 Hook 注册/最近观察的独立状态、旧服务端升级提示、真实 CLI 与 UAT 对账、完整尺寸与性能验收。默认 Rust 环境跳过项未因前端检查被标记通过。
