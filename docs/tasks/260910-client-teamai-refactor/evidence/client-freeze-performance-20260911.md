# 客户端版本固定与启动性能检查

2026-09-11。客户端代码提交 4d33d55d2a771f74d7f1a2ef86fc5f614d169953 已快进推送到远程 develop_hardie。提交包含明确的源码、第三方模块和任务证据，不包含 exe、数据库、环境文件或 Windows 诊断缓存。

提交前本轮检查：前端 147 文件/1181 项通过；Rust 2874 项通过、19 项默认跳过；类型检查、TeamAI 类型检查、clippy、no-default-features lib check 均通过；内置运行时 5 项测试通过。上游许可证的一处行尾空格为原始字节，为该文件单独记录 whitespace 属性，未改许可证内容。

客户端 GitNexus 已成功更新到 4d33d55d（25,346 nodes、58,501 edges）；动态 IPC 仍需实际测试，不能单凭图认定完成。

## 性能方法和结果

从 f811e352 的 git archive 单独构建相同 Windows x64 release 基线。两个构建结束后，交替启动基线和候选各 5 次，每次使用新的空应用数据/WebView 目录，PATH 仅 Windows 系统目录。就绪标准为 React 根非空且存在可见可用按钮；包含自动化探针开销。3 秒稳定后统计应用及子进程工作集之和，可能重复计算共享页；没有清空 OS 文件缓存。正常退出使用应用的 process exit API，没有强制终止。

| 指标中位数 | 基线 f811e352 | 候选 4d33d55d | 比例 |
| --- | --- | --- | --- |
| 观察到就绪 | 873.9 ms | 948.3 ms | 1.085 |
| 进程树工作集 | 450,314,240 bytes | 441,991,168 bytes | 0.982 |

本场景未超过 20% 回归阈值。首轮测量探针因浏览器守护进程继承输出管道而未正常返回，未纳入样本；改为文件收集输出后完整运行成功。原始样本和二进制哈希见 performance-4d33d55d.json。

这不证明已连接组织、大量资产、全部尺寸/语言/原生交互都通过。T20 整体验收仍需补齐对应场景。

候选安装包已从此提交成功构建，并保存在 target/deliverables/4d33d55d 下，含独立 manifest.json。UAT 当前为服务端 23a56574；登录后的审批、下载同步和管理员统计对账仍等待已授权测试身份。诊断副本清理的授权问题仍未解除。
